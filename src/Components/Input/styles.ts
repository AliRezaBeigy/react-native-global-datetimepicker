import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    width: 65,
    height: 70,
    fontSize: 38,
    borderRadius: 3,
    textAlign: 'center',
    paddingHorizontal: 10,
  },
});
